CodePen Link: https://codepen.io/bradleyhubbs/pen/abmvzoG?editors=1100
    
    1. What is Semantic HTML?                                   Semantic HTML is HTML that is used to for purpose rather than just presentation
    2. What is HTML used for?                                   Markup language used for organizing contents of a web page
    3. What is an attribute and where do we put it?             HTML attributes are additional information provided to elements
    4. What is the h1 tag used for?                             How many times should I use it on a page? H1 is the largest heading tag, it should only be used once for SEO
    5. Name two tags that have required attributes              The a tag and the img tag
    6. What do we put in the head of our HTML document?         Most relevant title/summary for the page
    7. What is an id?                                           an attribute used to specify an element, similiar to a class
    8. What elements can I add an id to?                        all elements        
    9. How many times can I use the same id on a page?          once
    10. What is a class?                                        an attribute used to specify an element, multiple elements can share a class
    11. What elements can I add a class to?                     every element
    12. How many times can I use the same class on a page?      You can use a class as many times as you'd like
    13. How do I get my link to open in a new tab?              the Target attribute
    14. What is the alt attribute in the image tag used for?    displays alternate text for a link
    15. How do I reference an id?                               #
    16. What is the difference between a section and a div      section should semantically represents a content group, div does not convey any meaning
    17. What is CSS used for?                                   CSS is used for styling web pages
    18. How to we select an element? Example - every h2 on the page             h2 { } on the CSS sheet
    19. What is the difference between a class and an id? -     id can only be used once, class can be used multiple times.  
    20. How do we select classes in CSS?                        .
    21. How do we select a p element with a single class of “human””?   .human p{ }
    22. What is a parent child selector? When would this be useful?     A selector containing other selectors.  Usefull for making changes to multiple selectors at the same time.
    23. How do you select all links within a div with the class of sidebar?     .sidebar a{ }
    24. What is a pseudo selector?                                     Used to define a special state of an element                                             
    25. What do we use the change the spacing between lines?            line-height property
    26. What do we use to change the spacing between letters?           letter-spacing property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?  text-transform: uppercase;lowercase;capitalize
    28. How do I add a 1px border around my div that is dotted and black?   border-style: dotted;
    29. How do I select everything on the page?                 ctrl a
    30. How do I write a comment in CSS?                        /*  */
    31. How do I find out what file I am in, when I am using the command line?  cd 
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm (careful because it removes files without a prompt)
    34. Why should I use version control?                       For backup safety of projects
    35. How often should I commit to github?                    When you make changes to a project
    36. What is the command we would use to push our repo up to github? git push -u origin firstname-lastname
    37. Walk me through Lambda's git flow.                      Fork -> Clone -> Branch -> Work on project -> Add -> Commit -> Push

Stretch Questions

    1. What is the difference between an inline element and a block element? block elements start on a new line, inline do not
    2. What happens when an element is positioned absolutely? an element with a fixed value
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 